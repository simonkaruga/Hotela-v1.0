services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: hotela
      POSTGRES_PASSWORD: hotela
      POSTGRES_DB: hotela
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.15.0
    restart: unless-stopped
    environment:
      discovery.type: single-node
      xpack.security.enabled: "false"
      ES_JAVA_OPTS: "-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - es_data:/usr/share/elasticsearch/data

  api:
    build:
      context: ./backend
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    env_file:
      - backend/apps/api/.env
    depends_on:
      - postgres
      - redis
    ports:
      - "4000:4000"

  ai-service:
    build:
      context: ./backend/apps/ai-service
    restart: unless-stopped
    env_file:
      - backend/apps/ai-service/.env
    depends_on:
      - postgres
    ports:
      - "8000:8000"

volumes:
  postgres_data:
  redis_data:
  es_data:
